version: 2.1
setup: true

jobs:
  trigger-dynamic:
    docker:
      - image: cimg/base:stable
    steps:
      - run:
          name: Import and Trigger Dynamic Config
          command: echo "Using dynamic.yml for workflow"

workflows:
  version: 2
  setup:
    jobs:
      - trigger-dynamic:
          config-path: .circleci/dynamic.yml